'use strict';
import GeocoderFactory from './lib/geocoderfactory';
// TODO: types options
export function getGeocoder(geocoderAdapter, options) {
    return GeocoderFactory.getGeocoder(geocoderAdapter, options);
}
